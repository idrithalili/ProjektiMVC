@model IEnumerable<Projekti.Models.Autore>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<br />
<br />
<br />
<br />
<br />
<br />

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmerAutori)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MbiemerAutori)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendbanimi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmerAutori)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MbiemerAutori)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vendbanimi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
