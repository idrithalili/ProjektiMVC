@model IEnumerable<Projekti.Models.BotimeLibrash>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdLibri)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumerKopjesh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Viti)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formati)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdLibri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumerKopjesh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Viti)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Formati)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBotimi">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdBotimi">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdBotimi">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
