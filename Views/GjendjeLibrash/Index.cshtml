@model IEnumerable<Projekti.Models.GjendjeLibrash>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumerKopjesh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gjendje)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumerKopjesh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gjendje)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLibri">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLibri">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLibri">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
